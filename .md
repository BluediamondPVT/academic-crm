 <button
                          onClick={() => handleDeleteStudent(student._id)}
                          disabled={deletingId === student._id}
                          className="p-2 text-gray-400 hover:text-red-600 hover:bg-red-50 rounded-lg transition-colors disabled:opacity-50"
                          title="Delete Student"
                        >
                          {deletingId === student._id ? (
                            <Loader2 className="h-4 w-4 animate-spin text-red-600" />
                          ) : (
                            <Trash2 className="h-4 w-4" />
                          )}
                        </button>





                        Bhadiya bhai! Ye rahe tere Phase-Wise Prompts.

In prompts ko maine is tarah design kiya hai ki tu inko direct copy karke mujhe (ya kisi bhi AI ko) dega, toh main ekdum exact wahi code generate karunga jo humne discuss kiya hai. Antigravity mode on! 🚀

Tujhe bas ek-ek karke ye prompts copy-paste karne hain:

1. Prompt for Phase 1: Database Architecture
Is prompt ko copy karke bhej, main database ka structure (Models) ready kar dunga.

Copy this:
"Bhai, Phase 1 start karte hain. Mujhe MongoDB (Mongoose) ka schema bana kar de 'Daily Wager Tracking' ke liye. Mujhe do cheezein chahiye:

Worker Schema (Name, Skill, PerHourRate, CurrentStatus, etc.)

DailyLogs Schema jisme ek din ki array of objects ho (Time, Action: IN/TRANSFER/OUT, SiteName, UpdatedBy, Remark).
Schema aisi honi chahiye jo Next.js serverless environment mein smoothly kaam kare aur total payment calculate karne mein aasaani ho."

2. Prompt for Phase 2: Core Backend Logic (APIs)
Phase 1 ka code set hone ke baad ye prompt use karna.

Copy this:
"Ab Phase 2 ki baari. Next.js App Router (app/api/...) ke liye CRUD APIs likh.

Ek API naye Worker ko add karne aur list fetch karne ke liye.

Ek Action API jahan manager IN, TRANSFER (Site change), ya OUT request bhejega.

Jab OUT ka action trigger ho, toh us din ke IN aur OUT time ke difference se 'Total Hours' aur 'Total Payment' (Total Hours * PerHourRate) calculate karne ka logic zaroor daalna."

3. Prompt for Phase 3: The HR Dashboard (Main Data Table)
API ready hone ke baad UI ka main skeleton banayenge.

Copy this:
"Phase 3 pe chalte hain. Frontend UI bana Next.js aur Tailwind CSS use karke.

Ek 'Total Workers' page bana jisme upar 'Add Worker' ka button ho.

Button pe click karne par ek Modal/Form khule jahan HR Name, Skill, aur Per Hour Rate daal sake.

Niche ek Table bana jisme columns hon: Name, Skill, Current Site, IN Time, OUT Time, Total Hrs, Total Payment, Updated Role, aur Actions (View/Edit). Table mein sirf latest action (Current Status) dikhna chahiye."

4. Prompt for Phase 4: Manager Action Flow (Edit & Remarks)
Managers ke liye actions aur form set karne ke liye.

Copy this:
"Phase 4 start kar. Table ke 'Edit' button ka logic aur Modal UI bana.
Jab koi manager edit pe click kare:

Ek modal khule jisme Dropdown ho Action select karne ka (IN, TRANSFER, OUT).

Agar IN ya TRANSFER hai, toh 'Site Name' daalne ka input aaye.

Ek text area ho 'Remark' (optional) ke liye.

Ye data submit hone par Phase 2 wali Action API ko hit kare aur table refresh ho jaye. UI ekdum clean aur mobile-friendly hona chahiye."

5. Prompt for Phase 5: The Tracker Masterpiece
Sabse last aur advanced step (Timeline & Calendar).

Copy this:
"Bhai aakhri Phase 5. Ab ek detailed 'View Page' bana.
Jab HR/Manager table ke 'View' button pe click kare toh ye khule.

Upar worker ki basic profile details dikha.

Ek Calendar laga jahan user specific date select kar sake.

Niche ek Zomato/Swiggy style Vertical Timeline (Progress tracker) bana. Jo date select hui hai, us din ka poora log (IN -> TRANSFER -> OUT) line se dikhna chahiye, jisme Time, Site, Manager ka naam aur uske Remarks show hon."

Tu bas apne IDE mein ek nayi branch bana, aur pehla prompt bhej. Hum shuru se end tak isko complete karte hain!