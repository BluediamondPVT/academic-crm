 <!-- <button
                          onClick={() => handleDeleteStudent(student._id)}
                          disabled={deletingId === student._id}
                          className="p-2 text-gray-400 hover:text-red-600 hover:bg-red-50 rounded-lg transition-colors disabled:opacity-50"
                          title="Delete Student"
                        >
                          {deletingId === student._id ? (
                            <Loader2 className="h-4 w-4 animate-spin text-red-600" />
                          ) : (
                            <Trash2 className="h-4 w-4" />
                          )}
                        </button>-->

Student Pay Slip 

Student Id
name 
number 
email 
University Name 
Course 
Total Fee 
paid Fee
Rest Fee 
payment Date (with Notes)


Stduent Sign
Counsellor Sign 


   

   <!-- //  academic Dashboard design  -->

   'use client';

import React, { useState, useEffect } from 'react';
import Link from 'next/link';
import {
  GraduationCap,
  Users,
  CheckCircle2,
  Headset,
  BookOpen,
  Calendar,
  AlertTriangle,
  ArrowUpRight,
  TrendingUp,
  RefreshCw,
  Search,
  ExternalLink,
  ChevronRight,
  PlusCircle,
  Eye,
  CreditCard,
  Building,
  Phone,
  Clock,
  Sparkles,
  Loader2,
  DollarSign
} from 'lucide-react';

// Demo / Dummy Data for Academic Dashboard
const DUMMY_METRICS = {
  confirmedAdmissions: 48,
  totalRevenue: 3450000,
  totalPaid: 2180000,
  totalOutstanding: 1270000,
  pipelineLeads: 142,
  partnerUniversities: 6,
  activeCourses: 34,
  dueAlertsCount: 5,
};

const DUMMY_ADMISSIONS = [
  {
    _id: 'dummy-1',
    name: 'Aarav Sharma',
    phoneNumber: '+91 98765 43210',
    email: 'aarav.sharma@example.com',
    universityName: 'Amity University Online',
    courseName: 'Master of Business Administration (MBA)',
    totalFee: 180000,
    totalPaid: 180000,
    remainingFee: 0,
    counselorName: 'Test Counselor',
    status: 'Admission',
    city: 'Delhi',
    createdAt: new Date(Date.now() - 1000 * 60 * 60 * 3).toISOString(),
  },
  {
    _id: 'dummy-2',
    name: 'Priya Patel',
    phoneNumber: '+91 98234 56789',
    email: 'priya.patel@example.com',
    universityName: 'Chandigarh University',
    courseName: 'Master of Computer Applications (MCA)',
    totalFee: 140000,
    totalPaid: 90000,
    remainingFee: 50000,
    counselorName: 'Sarah Jenkins',
    status: 'Admission',
    city: 'Mumbai',
    createdAt: new Date(Date.now() - 1000 * 60 * 60 * 8).toISOString(),
  },
  {
    _id: 'dummy-3',
    name: 'Mohammad Faizan',
    phoneNumber: '+91 97112 34567',
    email: 'faizan.m@example.com',
    universityName: 'Jain University Online',
    courseName: 'Bachelor of Computer Applications (BCA)',
    totalFee: 120000,
    totalPaid: 60000,
    remainingFee: 60000,
    counselorName: 'Test Counselor',
    status: 'Admission',
    city: 'Hyderabad',
    createdAt: new Date(Date.now() - 1000 * 60 * 60 * 24).toISOString(),
  },
  {
    _id: 'dummy-4',
    name: 'Sneha Kulkarni',
    phoneNumber: '+91 99456 78901',
    email: 'sneha.k@example.com',
    universityName: 'Manipal University Jaipur',
    courseName: 'Bachelor of Business Admin (BBA)',
    totalFee: 110000,
    totalPaid: 110000,
    remainingFee: 0,
    counselorName: 'Super Admin',
    status: 'Admission',
    city: 'Bangalore',
    createdAt: new Date(Date.now() - 1000 * 60 * 60 * 36).toISOString(),
  },
  {
    _id: 'dummy-5',
    name: 'Rohan Deshmukh',
    phoneNumber: '+91 96543 21098',
    email: 'rohan.d@example.com',
    universityName: 'NMIMS Global Access',
    courseName: 'PG Diploma in Data Science',
    totalFee: 95000,
    totalPaid: 45000,
    remainingFee: 50000,
    counselorName: 'Sarah Jenkins',
    status: 'Admission',
    city: 'Pune',
    createdAt: new Date(Date.now() - 1000 * 60 * 60 * 48).toISOString(),
  },
  {
    _id: 'dummy-6',
    name: 'Ananya Verma',
    phoneNumber: '+91 98123 45678',
    email: 'ananya.v@example.com',
    universityName: 'Amity University Online',
    courseName: 'MA in Journalism & Mass Comm',
    totalFee: 105000,
    totalPaid: 65000,
    remainingFee: 40000,
    counselorName: 'Test Counselor',
    status: 'Admission',
    city: 'Lucknow',
    createdAt: new Date(Date.now() - 1000 * 60 * 60 * 72).toISOString(),
  },
];

const DUMMY_DUE_ALERTS = [
  {
    id: 'due-1',
    studentName: 'Priya Patel',
    course: 'MCA (Chandigarh Univ)',
    amount: 50000,
    dueDate: '10 Sep 2026',
    diffDays: 8,
    isOverdue: false,
    counselor: 'Sarah Jenkins',
  },
  {
    id: 'due-2',
    studentName: 'Mohammad Faizan',
    course: 'BCA (Jain Univ)',
    amount: 60000,
    dueDate: '01 Sep 2026',
    diffDays: -1,
    isOverdue: true,
    counselor: 'Test Counselor',
  },
  {
    id: 'due-3',
    studentName: 'Rohan Deshmukh',
    course: 'PG Diploma Data Science (NMIMS)',
    amount: 50000,
    dueDate: '15 Sep 2026',
    diffDays: 13,
    isOverdue: false,
    counselor: 'Sarah Jenkins',
  },
  {
    id: 'due-4',
    studentName: 'Ananya Verma',
    course: 'MA Journalism (Amity)',
    amount: 40000,
    dueDate: '20 Sep 2026',
    diffDays: 18,
    isOverdue: false,
    counselor: 'Test Counselor',
  },
  {
    id: 'due-5',
    studentName: 'Karan Mehra',
    course: 'Executive MBA (Symbiosis)',
    amount: 75000,
    dueDate: '28 Aug 2026',
    diffDays: -5,
    isOverdue: true,
    counselor: 'Sarah Jenkins',
  },
];

const DUMMY_UNIVERSITIES = [
  {
    id: 'uni-1',
    name: 'Amity University Online',
    location: 'Noida, UP',
    modeOfLearning: 'Online / Distance',
    coursesCount: 12,
  },
  {
    id: 'uni-2',
    name: 'Chandigarh University',
    location: 'Mohali, Punjab',
    modeOfLearning: 'Online Learning',
    coursesCount: 8,
  },
  {
    id: 'uni-3',
    name: 'Jain University Online',
    location: 'Bangalore, Karnataka',
    modeOfLearning: 'Online Mode',
    coursesCount: 7,
  },
  {
    id: 'uni-4',
    name: 'Manipal University Jaipur',
    location: 'Jaipur, Rajasthan',
    modeOfLearning: 'Online / Distance',
    coursesCount: 5,
  },
  {
    id: 'uni-5',
    name: 'NMIMS Global Access',
    location: 'Mumbai, Maharashtra',
    modeOfLearning: 'Distance Learning',
    coursesCount: 6,
  },
];

export default function AcademicDashboardPage() {
  const [loading, setLoading] = useState(false);
  const [refreshing, setRefreshing] = useState(false);

  // Live or fallback data
  const [admissions, setAdmissions] = useState(DUMMY_ADMISSIONS);
  const [universities, setUniversities] = useState(DUMMY_UNIVERSITIES);
  const [dueAlerts, setDueAlerts] = useState(DUMMY_DUE_ALERTS);

  const [metrics, setMetrics] = useState({
    confirmedCount: DUMMY_METRICS.confirmedAdmissions,
    totalPaid: DUMMY_METRICS.totalPaid,
    totalRevenue: DUMMY_METRICS.totalRevenue,
    pipelineLeads: DUMMY_METRICS.pipelineLeads,
    partnerUnis: DUMMY_METRICS.partnerUniversities,
    totalCourses: DUMMY_METRICS.activeCourses,
    dueAlertsCount: DUMMY_METRICS.dueAlertsCount,
  });

  useEffect(() => {
    fetchLiveData();
  }, []);

  const fetchLiveData = async () => {
    try {
      const [studentsRes, uniRes] = await Promise.all([
        fetch('/api/students'),
        fetch('/api/admin/universities'),
      ]);

      if (studentsRes.ok && uniRes.ok) {
        const studentsData = await studentsRes.json();
        const uniData = await uniRes.json();

        if (Array.isArray(studentsData) && studentsData.length > 0) {
          const confirmed = studentsData.filter((s: any) => s.status === 'Admission');
          const leads = studentsData.filter((s: any) => s.status !== 'Admission');
          const paidSum = confirmed.reduce((acc: number, s: any) => acc + (s.totalPaid || 0), 0);
          const revenueSum = confirmed.reduce((acc: number, s: any) => acc + (s.totalFee || 0), 0);

          if (confirmed.length > 0) {
            setAdmissions(confirmed.slice(0, 6));
          }

          const coursesTotal = Array.isArray(uniData)
            ? uniData.reduce((acc: number, u: any) => acc + (Array.isArray(u.courses) ? u.courses.length : 0), 0)
            : DUMMY_METRICS.activeCourses;

          if (Array.isArray(uniData) && uniData.length > 0) {
            setUniversities(
              uniData.map((u: any) => ({
                id: u._id,
                name: u.name,
                location: u.location,
                modeOfLearning: u.modeOfLearning || 'Online',
                coursesCount: Array.isArray(u.courses) ? u.courses.length : 0,
              }))
            );
          }

          setMetrics({
            confirmedCount: confirmed.length || DUMMY_METRICS.confirmedAdmissions,
            totalPaid: paidSum || DUMMY_METRICS.totalPaid,
            totalRevenue: revenueSum || DUMMY_METRICS.totalRevenue,
            pipelineLeads: leads.length || DUMMY_METRICS.pipelineLeads,
            partnerUnis: Array.isArray(uniData) && uniData.length > 0 ? uniData.length : DUMMY_METRICS.partnerUniversities,
            totalCourses: coursesTotal || DUMMY_METRICS.activeCourses,
            dueAlertsCount: dueAlerts.length,
          });
        }
      }
    } catch (e) {
      console.log('Using dummy preview data for academic dashboard');
    } finally {
      setRefreshing(false);
    }
  };

  const handleRefresh = () => {
    setRefreshing(true);
    fetchLiveData();
    setTimeout(() => setRefreshing(false), 800);
  };

  return (
    <div className="space-y-6 font-sans text-gray-800 pb-12 animate-in fade-in duration-200">
      {/* Top Welcome Banner */}
      <div className="bg-gradient-to-r from-slate-900 via-indigo-950 to-purple-950 rounded-2xl p-6 text-white shadow-md relative overflow-hidden">
        <div className="absolute top-0 right-0 -mt-8 -mr-8 w-64 h-64 bg-purple-500/10 rounded-full blur-3xl pointer-events-none" />
        <div className="absolute bottom-0 left-1/3 -mb-10 w-48 h-48 bg-indigo-500/10 rounded-full blur-2xl pointer-events-none" />

        <div className="relative z-10 flex flex-col md:flex-row justify-between items-start md:items-center gap-4">
          <div>
            <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-purple-500/20 text-purple-300 text-xs font-semibold mb-2 backdrop-blur-sm border border-purple-500/30">
              <GraduationCap className="h-3.5 w-3.5" />
              <span>Academic Operations Console</span>
            </div>
            <h1 className="text-2xl font-extrabold tracking-tight text-white flex items-center gap-2">
              Academic Dashboard
            </h1>
            <p className="mt-1 text-xs text-slate-300 max-w-xl">
              Monitor university admissions, fee fulfillment, partner university catalogs, and installment recovery.
            </p>
          </div>

          <div className="flex items-center gap-3">
            <button
              type="button"
              onClick={handleRefresh}
              disabled={refreshing}
              className="flex items-center gap-1.5 px-3.5 py-2 rounded-xl bg-white/10 hover:bg-white/20 text-white text-xs font-semibold backdrop-blur-sm border border-white/10 transition-all cursor-pointer disabled:opacity-50"
            >
              <RefreshCw className={`h-3.5 w-3.5 ${refreshing ? 'animate-spin' : ''}`} />
              <span>{refreshing ? 'Syncing...' : 'Refresh Data'}</span>
            </button>

            <Link
              href="/admissions"
              className="flex items-center gap-1.5 px-4 py-2 rounded-xl bg-purple-600 hover:bg-purple-700 text-white text-xs font-bold shadow-md shadow-purple-600/30 transition-all cursor-pointer"
            >
              <span>View Admissions</span>
              <ArrowUpRight className="h-3.5 w-3.5" />
            </Link>
          </div>
        </div>
      </div>

      {/* KPI Metrics Cards */}
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
        {/* Card 1: Confirmed Admissions */}
        <div className="bg-white rounded-2xl p-5 shadow-xs border border-gray-100 hover:shadow-md transition-all">
          <div className="flex items-center justify-between">
            <span className="text-xs font-bold text-gray-500 uppercase tracking-wider">
              Confirmed Admissions
            </span>
            <div className="p-2.5 rounded-xl bg-emerald-50 text-emerald-600">
              <CheckCircle2 className="h-5 w-5" />
            </div>
          </div>
          <div className="mt-3">
            <div className="text-2xl font-black text-[#112a46]">
              {metrics.confirmedCount}
            </div>
            <div className="flex items-center gap-1.5 mt-1 text-xs text-emerald-600 font-semibold">
              <TrendingUp className="h-3.5 w-3.5" />
              <span>₹{metrics.totalPaid.toLocaleString('en-IN')} collected</span>
            </div>
          </div>
        </div>

        {/* Card 2: Active Enquiries / Leads */}
        <div className="bg-white rounded-2xl p-5 shadow-xs border border-gray-100 hover:shadow-md transition-all">
          <div className="flex items-center justify-between">
            <span className="text-xs font-bold text-gray-500 uppercase tracking-wider">
              Pipeline Leads
            </span>
            <div className="p-2.5 rounded-xl bg-indigo-50 text-indigo-600">
              <Headset className="h-5 w-5" />
            </div>
          </div>
          <div className="mt-3">
            <div className="text-2xl font-black text-[#112a46]">
              {metrics.pipelineLeads}
            </div>
            <p className="mt-1 text-xs text-gray-400 font-medium">
              Prospective students in follow-up
            </p>
          </div>
        </div>

        {/* Card 3: Partner Universities & Courses */}
        <div className="bg-white rounded-2xl p-5 shadow-xs border border-gray-100 hover:shadow-md transition-all">
          <div className="flex items-center justify-between">
            <span className="text-xs font-bold text-gray-500 uppercase tracking-wider">
              Universities & Courses
            </span>
            <div className="p-2.5 rounded-xl bg-purple-50 text-purple-600">
              <Building className="h-5 w-5" />
            </div>
          </div>
          <div className="mt-3">
            <div className="text-2xl font-black text-[#112a46]">
              {metrics.partnerUnis}
            </div>
            <p className="mt-1 text-xs text-purple-600 font-semibold">
              {metrics.totalCourses} active course offerings
            </p>
          </div>
        </div>

        {/* Card 4: Due Date Alerts */}
        <div className="bg-white rounded-2xl p-5 shadow-xs border border-gray-100 hover:shadow-md transition-all">
          <div className="flex items-center justify-between">
            <span className="text-xs font-bold text-gray-500 uppercase tracking-wider">
              Fee Installment Alerts
            </span>
            <div className="p-2.5 rounded-xl bg-amber-50 text-amber-600">
              <AlertTriangle className="h-5 w-5" />
            </div>
          </div>
          <div className="mt-3">
            <div className="text-2xl font-black text-[#112a46]">
              {metrics.dueAlertsCount}
            </div>
            <p className="mt-1 text-xs text-amber-600 font-semibold">
              Requires follow-up within 30 days
            </p>
          </div>
        </div>
      </div>

      {/* Quick Navigation Cards */}
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3">
        <Link
          href="/admissions"
          className="bg-white p-4 rounded-2xl border border-gray-100 hover:border-purple-200 hover:shadow-sm transition-all flex items-center justify-between group"
        >
          <div className="flex items-center gap-3">
            <div className="p-2 rounded-xl bg-purple-50 text-purple-700 group-hover:bg-purple-600 group-hover:text-white transition-all">
              <CheckCircle2 className="h-4 w-4" />
            </div>
            <div>
              <h3 className="text-xs font-extrabold text-slate-800">Confirmed Admissions</h3>
              <p className="text-[10px] text-gray-400">View enrolled student ledger</p>
            </div>
          </div>
          <ChevronRight className="h-4 w-4 text-gray-400 group-hover:text-purple-600 group-hover:translate-x-0.5 transition-all" />
        </Link>

        <Link
          href="/admin/universities"
          className="bg-white p-4 rounded-2xl border border-gray-100 hover:border-indigo-200 hover:shadow-sm transition-all flex items-center justify-between group"
        >
          <div className="flex items-center gap-3">
            <div className="p-2 rounded-xl bg-indigo-50 text-indigo-700 group-hover:bg-indigo-600 group-hover:text-white transition-all">
              <Building className="h-4 w-4" />
            </div>
            <div>
              <h3 className="text-xs font-extrabold text-slate-800">Partner Universities</h3>
              <p className="text-[10px] text-gray-400">Manage institutes & courses</p>
            </div>
          </div>
          <ChevronRight className="h-4 w-4 text-gray-400 group-hover:text-indigo-600 group-hover:translate-x-0.5 transition-all" />
        </Link>

        <Link
          href="/admin/students"
          className="bg-white p-4 rounded-2xl border border-gray-100 hover:border-blue-200 hover:shadow-sm transition-all flex items-center justify-between group"
        >
          <div className="flex items-center gap-3">
            <div className="p-2 rounded-xl bg-blue-50 text-blue-700 group-hover:bg-blue-600 group-hover:text-white transition-all">
              <Headset className="h-4 w-4" />
            </div>
            <div>
              <h3 className="text-xs font-extrabold text-slate-800">Student Pipeline</h3>
              <p className="text-[10px] text-gray-400">All student enquiries & leads</p>
            </div>
          </div>
          <ChevronRight className="h-4 w-4 text-gray-400 group-hover:text-blue-600 group-hover:translate-x-0.5 transition-all" />
        </Link>

        <Link
          href="/counselor/leads/create"
          className="bg-white p-4 rounded-2xl border border-gray-100 hover:border-emerald-200 hover:shadow-sm transition-all flex items-center justify-between group"
        >
          <div className="flex items-center gap-3">
            <div className="p-2 rounded-xl bg-emerald-50 text-emerald-700 group-hover:bg-emerald-600 group-hover:text-white transition-all">
              <PlusCircle className="h-4 w-4" />
            </div>
            <div>
              <h3 className="text-xs font-extrabold text-slate-800">New Registration</h3>
              <p className="text-[10px] text-gray-400">Register new lead or admission</p>
            </div>
          </div>
          <ChevronRight className="h-4 w-4 text-gray-400 group-hover:text-emerald-600 group-hover:translate-x-0.5 transition-all" />
        </Link>
      </div>

      {/* Main 2-Column Section */}
      <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
        {/* Left Column (2 spans): Recent Confirmed Admissions */}
        <div className="lg:col-span-2 space-y-4">
          <div className="bg-white rounded-2xl p-5 shadow-xs border border-gray-100">
            <div className="flex items-center justify-between pb-3 border-b border-gray-100 mb-4">
              <div className="flex items-center gap-2">
                <div className="p-1.5 rounded-lg bg-emerald-50 text-emerald-600">
                  <CheckCircle2 className="h-4 w-4" />
                </div>
                <div>
                  <h2 className="text-sm font-bold text-slate-800">Recent Confirmed Admissions</h2>
                  <p className="text-[11px] text-gray-400">Latest enrolled students across partner universities</p>
                </div>
              </div>

              <Link
                href="/admissions"
                className="text-xs font-bold text-purple-600 hover:text-purple-700 flex items-center gap-1"
              >
                <span>View All</span>
                <ChevronRight className="h-3.5 w-3.5" />
              </Link>
            </div>

            <div className="overflow-x-auto">
              <table className="w-full text-left border-collapse">
                <thead>
                  <tr className="bg-gray-50/70">
                    <th className="px-3 py-2.5 text-[10px] font-bold text-gray-500 uppercase tracking-wider rounded-l-xl">Student</th>
                    <th className="px-3 py-2.5 text-[10px] font-bold text-gray-500 uppercase tracking-wider">University &amp; Course</th>
                    <th className="px-3 py-2.5 text-[10px] font-bold text-gray-500 uppercase tracking-wider">Fee Progress</th>
                    <th className="px-3 py-2.5 text-[10px] font-bold text-gray-500 uppercase tracking-wider">Counselor</th>
                    <th className="px-3 py-2.5 text-[10px] font-bold text-gray-500 uppercase tracking-wider text-right rounded-r-xl">Action</th>
                  </tr>
                </thead>
                <tbody className="divide-y divide-gray-100">
                  {admissions.map((student) => {
                    const total = student.totalFee || 0;
                    const paid = student.totalPaid || 0;
                    const percent = total > 0 ? Math.min(100, Math.round((paid / total) * 100)) : 0;

                    return (
                      <tr key={student._id} className="hover:bg-slate-50/60 transition-colors">
                        <td className="px-3 py-3">
                          <div className="text-xs font-bold text-slate-800">{student.name}</div>
                          <div className="text-[10px] text-gray-400 flex items-center gap-1 mt-0.5">
                            <Phone className="h-2.5 w-2.5" />
                            <span>{student.phoneNumber}</span>
                          </div>
                        </td>
                        <td className="px-3 py-3">
                          <div className="text-xs font-semibold text-slate-700">{student.courseName}</div>
                          <div className="text-[10px] text-indigo-600 font-bold uppercase mt-0.5">
                            {student.universityName}
                          </div>
                        </td>
                        <td className="px-3 py-3">
                          <div className="flex items-center justify-between text-[10px] font-semibold text-slate-700 mb-1">
                            <span>₹{paid.toLocaleString('en-IN')}</span>
                            <span className="text-gray-400">of ₹{total.toLocaleString('en-IN')}</span>
                          </div>
                          <div className="w-24 bg-gray-100 rounded-full h-1.5 overflow-hidden">
                            <div
                              className={`h-1.5 rounded-full ${percent >= 100
                                  ? 'bg-emerald-500'
                                  : percent >= 50
                                    ? 'bg-indigo-500'
                                    : 'bg-amber-500'
                                }`}
                              style={{ width: `${percent}%` }}
                            />
                          </div>
                        </td>
                        <td className="px-3 py-3">
                          <span className="inline-flex items-center px-2 py-0.5 rounded-md text-[10px] font-bold bg-slate-100 text-slate-700">
                            {student.counselorName || 'Super Admin'}
                          </span>
                        </td>
                        <td className="px-3 py-3 text-right">
                          <Link
                            href={`/counselor/leads/view/${student._id}?from=academic`}
                            className="p-1.5 text-gray-400 hover:text-purple-600 hover:bg-purple-50 rounded-lg inline-block transition-all"
                            title="View Details"
                          >
                            <Eye className="h-3.5 w-3.5" />
                          </Link>
                        </td>
                      </tr>
                    );
                  })}
                </tbody>
              </table>
            </div>
          </div>
        </div>

        {/* Right Column (1 span): Due Date Alerts & University Catalog */}
        <div className="space-y-4">
          {/* Due Date Installment Alerts */}
          <div className="bg-white rounded-2xl p-5 shadow-xs border border-gray-100">
            <div className="flex items-center justify-between pb-3 border-b border-gray-100 mb-3">
              <div className="flex items-center gap-2">
                <div className="p-1.5 rounded-lg bg-amber-50 text-amber-600">
                  <AlertTriangle className="h-4 w-4" />
                </div>
                <div>
                  <h2 className="text-xs font-bold text-slate-800">Fee Due Alerts</h2>
                  <p className="text-[10px] text-gray-400">Installments due soon or overdue</p>
                </div>
              </div>
              <span className="text-[10px] font-bold px-2 py-0.5 rounded-full bg-amber-50 text-amber-700 border border-amber-200">
                {dueAlerts.length} Students
              </span>
            </div>

            <div className="space-y-2.5 max-h-64 overflow-y-auto pr-1">
              {dueAlerts.map((item) => (
                <div
                  key={item.id}
                  className={`p-2.5 rounded-xl border text-xs flex items-center justify-between ${item.isOverdue
                      ? 'bg-red-50/70 border-red-200 text-red-800'
                      : 'bg-amber-50/70 border-amber-200 text-amber-800'
                    }`}
                >
                  <div>
                    <div className="font-bold flex items-center gap-1.5">
                      <span>{item.studentName}</span>
                      <span
                        className={`text-[9px] px-1.5 py-0.2 rounded font-extrabold uppercase ${item.isOverdue
                            ? 'bg-red-200 text-red-800'
                            : 'bg-amber-200 text-amber-800'
                          }`}
                      >
                        {item.isOverdue ? 'Overdue' : `${item.diffDays}d Left`}
                      </span>
                    </div>
                    <div className="text-[10px] text-gray-600 mt-0.5">
                      {item.course} • Due: {item.dueDate}
                    </div>
                  </div>

                  <div className="text-right">
                    <div className="font-extrabold text-xs">
                      ₹{item.amount.toLocaleString('en-IN')}
                    </div>
                    <Link
                      href="/admissions"
                      className="text-[9px] font-bold text-indigo-600 hover:underline inline-block mt-0.5"
                    >
                      Follow up &rarr;
                    </Link>
                  </div>
                </div>
              ))}
            </div>
          </div>

          {/* Partner Universities Catalog Summary */}
          <div className="bg-white rounded-2xl p-5 shadow-xs border border-gray-100">
            <div className="flex items-center justify-between pb-3 border-b border-gray-100 mb-3">
              <div className="flex items-center gap-2">
                <div className="p-1.5 rounded-lg bg-indigo-50 text-indigo-600">
                  <Building className="h-4 w-4" />
                </div>
                <div>
                  <h2 className="text-xs font-bold text-slate-800">Partner Universities</h2>
                  <p className="text-[10px] text-gray-400">Offerings &amp; learning modes</p>
                </div>
              </div>

              <Link
                href="/admin/universities"
                className="text-[10px] font-bold text-indigo-600 hover:underline"
              >
                Manage
              </Link>
            </div>

            <div className="space-y-2 max-h-56 overflow-y-auto pr-1">
              {universities.map((uni) => (
                <div
                  key={uni.id}
                  className="p-2.5 rounded-xl bg-slate-50 border border-slate-100 flex items-center justify-between text-xs"
                >
                  <div>
                    <h3 className="font-bold text-slate-800">{uni.name}</h3>
                    <div className="text-[10px] text-gray-400 flex items-center gap-1.5 mt-0.5">
                      <span>{uni.location}</span>
                      <span>•</span>
                      <span className="font-semibold text-indigo-600">{uni.modeOfLearning}</span>
                    </div>
                  </div>
                  <span className="text-[10px] font-bold px-2 py-0.5 rounded-lg bg-white border border-gray-200 text-slate-700">
                    {uni.coursesCount} Courses
                  </span>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}

